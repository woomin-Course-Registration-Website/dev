-- ── Flyway V1: 초기 스키마 ─────────────────────────────────────────────────
-- 이 파일은 JPA 엔티티에서 자동 생성합니다.
--
-- 첫 prod 배포 전 1회 실행:
--   ./gradlew bootRun --args='--spring.profiles.active=schemadump'
-- → 본 파일이 엔티티 기반 CREATE TABLE 문으로 채워집니다.
-- → 내용 검토(인덱스·FK·제약 추가 등) 후 git 커밋.
--
-- 이후 스키마 변경은 V2__설명.sql, V3__설명.sql 형태로 추가합니다.
-- ────────────────────────────────────────────────────────────────────────────
