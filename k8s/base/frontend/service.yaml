apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: student-mgmt
  annotations:
    alb.ingress.kubernetes.io/healthcheck-path: /healthz
spec:
  selector:
    app: frontend
  ports:
  - port: 80
    targetPort: 80
  type: ClusterIP
