apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: student-mgmt
  annotations:
    alb.ingress.kubernetes.io/healthcheck-path: /api/health
spec:
  selector:
    app: backend
  ports:
  - port: 8080
    targetPort: 8080
  type: ClusterIP
